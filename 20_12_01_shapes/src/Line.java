

public class Line extends Shape {

    private int length;

    public Line(char symbol) {
        super(symbol);
        this.length = length;
    }

    @Override
    public void draw() {

    }
}